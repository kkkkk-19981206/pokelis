{
  "name": "pokelis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check app.js && node --check insta.js && node --check api/generate.js && node --check api/identify.js"
  },
  "engines": {
    "node": ">=20"
  }
}
